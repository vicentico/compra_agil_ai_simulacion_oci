# /src

Código fuente — vacío por diseño en FASE 0 (regla principal MP2 §1: no comenzar escribiendo código). Estructura prevista (MP §37): `/apps` (frontend), `/services` (Platform API modular), `/workers` (sync, document, ai), `/building-blocks`, `/contracts`. Se crea en FASE 1.
