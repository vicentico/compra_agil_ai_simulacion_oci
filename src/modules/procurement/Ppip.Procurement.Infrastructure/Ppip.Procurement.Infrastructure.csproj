<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>Ppip.Procurement.Infrastructure</RootNamespace>
  </PropertyGroup>
  <ItemGroup>
    <!-- Versión flotante intencional (mismo criterio que los demás building
         blocks/módulos): se fija exacta vía Central Package Management
         cuando exista un pipeline de build real (FASE 4+ ya lo anunció). -->
    <PackageReference Include="Microsoft.Extensions.Http.Resilience" Version="10.*" />
    <PackageReference Include="MongoDB.Driver" Version="3.*" />
    <PackageReference Include="RabbitMQ.Client" Version="7.*" />
    <PackageReference Include="StackExchange.Redis" Version="2.*" />
  </ItemGroup>
  <ItemGroup>
    <!-- FASE 6: los adaptadores de persistencia (Mongo) e IChileCompraClient
         comparten este proyecto; implementan los puertos de
         Ppip.Procurement.Domain/Ports (ICompraAgilRepository, ISyncLock,
         etc.) — a diferencia de FASE 5, ahora SÍ hace falta conocer los
         agregados de dominio para poder reconstruirlos desde MongoDB. -->
    <ProjectReference Include="..\Ppip.Procurement.Domain\Ppip.Procurement.Domain.csproj" />
    <ProjectReference Include="..\..\..\building-blocks\Ppip.BuildingBlocks.Messaging\Ppip.BuildingBlocks.Messaging.csproj" />
  </ItemGroup>
</Project>
