# 09 — Preguntas abiertas

| ID | Pregunta | Bloquea | Resolver antes de | Estado |
|---|---|---|---|---|
| OQ-01 | ¿Formato exacto de respuesta y paginación de la API Compra Ágil v2 (nombres de campos, ventanas de cambio)? | Diseño fino del connector | FASE 5 | **Cerrada (2026-08-16)** — spike real ejecutado contra `https://api2.mercadopublico.cl`. Contrato completo en `src/modules/procurement/Ppip.Procurement.Infrastructure/ChileCompra/`; discrepancias reales vs. la Guía de Uso v3.0 documentadas en `docs/ROADMAP.md` nota de cierre de FASE 5. |
| OQ-02 | ¿Los adjuntos requieren autenticación para descarga? ¿URLs firmadas/temporales? | Pipeline de descarga | FASE 7 | Abierta — el spike de FASE 5 confirmó que `documentos[].id` existe (numérico, no UUID como documenta la guía) y `documentos[].nombre` trae el nombre del archivo, pero **no** se probó ningún endpoint de descarga (no documentado en la Guía de Uso v2) para no gastar cuota en algo fuera del alcance de esta fase — se resuelve en el spike de descarga de FASE 7. |
| OQ-03 | ¿Qué modelo de embeddings usar por defecto (local vs API) y con qué dimensión? | Colección Qdrant (dimensión fija) | FASE 9 | Abierta — candidatos: nomic-embed-text (Ollama), text-embedding-3-small |
| OQ-04 | ¿PostgreSQL entra desde FASE 1 o la auditoría inicia en MongoDB y migra después? | Data architecture | FASE 4 | Propuesta: iniciar en MongoDB, PostgreSQL al llegar reporting (ADR-002) |
| OQ-05 | ¿Plantilla de propuesta única o múltiples plantillas por rubro? | Proposal Generator | FASE 13 | Propuesta: una plantilla parametrizable en POC |
| OQ-06 | ¿Términos de uso de Mercado Público permiten almacenamiento y procesamiento IA de documentos? | Legal | FASE 5 | Parcialmente informada — el usuario aceptó los términos y condiciones de uso de la API al solicitar su ticket (paso obligatorio del flujo oficial). Revisión legal formal del alcance de esos términos (almacenamiento local + procesamiento IA de documentos, no solo consumo de la API) queda **pendiente**, no es analizable por una revisión de ingeniería (ASM-08 sigue abierta). |
| OQ-07 | ¿Qué gateway concreto: Traefik, Kong o YARP? | Infraestructura | FASE 1 | Propuesta: Traefik (ADR-009) |
| OQ-08 | ¿Idioma de la UI solo español o i18n desde el inicio? | Frontend | FASE 16 | Propuesta: español, estructura i18n-ready |
| OQ-09 | ¿Contra qué taxonomía se hace el matching de rubros: categorías/rubros que expone la API Compra Ágil (¿UNSPSC/ONU?) o taxonomía propia mapeada? | Matching (FR-057) | FASE 12 | Parcialmente informada por el spike de FASE 5: cada ítem de `productos_solicitados[]` trae `codigo_producto` (numérico, 8 dígitos en el ejemplo real capturado, p.ej. `51161639`) — formato compatible con UNSPSC, sin confirmar contra un catálogo oficial. No hay ningún campo `rubro`/`categoria` explícito en list ni detail. Confirmar el catálogo exacto queda para FASE 12. |
| OQ-10 | ¿La API pública expone adjudicaciones/órdenes de compra de Compras Ágiles consultables por proceso? Determina si el resultado de propuestas (FR-059) y el rastreo proactivo (FR-063) pueden automatizarse o quedan solo manuales | Efectividad (FR-059/063) | FASE 5 (spike) | **Cerrada (2026-08-16)** — sí: el detalle expone `id_orden_compra` (distinto de `null` cuando se emitió una OC), cruzable con la API de Órdenes de Compra de Mercado Público. Habilita automatizar FR-059/FR-063 en vez de depender solo de registro manual. Nota: la Guía de Uso v2 documenta este campo anidado bajo `orden_compra.*`, pero el spike lo observó como campo raíz del payload (`id_orden_compra` directo) — ver discrepancias en `docs/ROADMAP.md`. |

Regla: ninguna pregunta se cierra silenciosamente; su resolución se registra aquí y, si cambia una decisión, en un ADR.
